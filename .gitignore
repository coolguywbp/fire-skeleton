bin/
obj/
.build/
game
# Web build outputs are generated; keep only the hand-written shell (a build
# input). The vendored wasm SDL3/Lua stacks ARE committed so `make web` works
# from a fresh clone.
web/*
!web/shell.html
Graphics.af*

# Source recordings (the lightweight GIF in assets/ is committed instead)
*.mp4
*.mov
*.mkv
*.webm

*.env
*.sh
*.txt
*.c1
*.h1
